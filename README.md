"no readme" so far but here's how to build it i guess

run this command in terminal (if you're not on linux then just do kyllr.exe instead of kyllr.game)  
```odin build . -out:build/kyllr.game -o:speed```
